[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

# SPA fallback: send all routes to index.html so client-side router can handle them
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 